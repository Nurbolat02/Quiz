импортируем сюда стили
КвизСлайдер
Футер
таймер
Хук для получения доступа к ссылке через которую мы попали на саму страницу
КвизСервис
Хук состояния, хук для ссылок, Юз эффект, юз колбЭк

Инициализируем собственно наш Questions, далее
АЙДИ, который мы получаем из ссылки по которой юзер перешел на данную страницу
Создаем стейт-флаг для таймер (стопТаймер) который по дефолту равен фолс
Создаем стейт для таймера в котором будет последнее значение секундомера, по дефолту оно равно 0
Создаем стест для данных, по дефолту эту пустой массив
Достаем метод для получения Квиза по айди
Инициализируем функцию кол бэк для отправки запроса на сервер и записи ответа в данные.
Через кол бэк просто чтобы в случае рендера эта функция не выполнялась снова
В кол Бэке в качестве зависимостей мы указываем список значений, от которых зависит сама функция.

Далее в хуке ЮзЭффект мы вызываем написанный заранее колБэк для получения данных

напишем функцию для получения финального времени таймера КОГДА мы его остановим

Напишем функцию для остановки самого таймера (имзенения его стейта-флага)

Возвращаем JSX 
В компонент Таймера передаем стейт-флаг состояния рабоыт таймера + функцию для получения финального времени

В компонент Слайдера передаем АЙДИ Квиза, функцию для остановки самого таймера и само конечное время

Timer


Таймер принимает пропс СТОП который по дефолту равен нулю, а также функцию для получения финального времени 

достаем из контекста функцию для форматирования времени.
Чтобы сохранять время, изменять его и при этом не вызывать рендеринг мы использую хук useRef c дефолтным значением равным 0.
Для визуального отображения мы будем использовать обычынй стейт с дефолтным значением 0.

В ЮзЭффект пишем проверку флага-состояния.Если оно тру, то вызываем функцию для получения финального времени и собственно передаем туда время из нашего useRef

Иначе если проверку мы не прошли (флаг-состояния равен фолс), то создаем переменную в которую будем запихивать наш интервал, в котором каждую секунду значение из useRef будет увеличиваться на 1 + время отоброжающееся для юзера будет также обновляться на значение из useRef.

Не забудь написать очистку таймера и зависимость о флага-состояния + самой используемой внутри функции.

возвращаем JSX где в функцию для форматирования времени мы передаем время для дисплея


QuizSlider 
В QuizSlider передаем в качестве пропсов АЙДИ + функцию для остановки таймера + конечное время

Создаем стейты для
СлайдИндекс (0)
Ответы ({})
Результат (null)
и прописываем пару функций для
Перехода к следующему слайду nextSlide
Перехода к предыдущему слайду prevSlide
Перехода к слайду по индексу 

Опишем функцию для обработки выбранных ответов, которая принимает questionId + optionIndex
Внутри мы обращаемся к состоянию ответов, передаем прошлое состояние, деструктурируем его и добавляем 
ключ questionId со значением optionIndex

Опишем функцию для подсчета правильных ответов в тесте.
Инициализируем счетчик с изначальным значением равным нулю.
Пройдемся по каждому элементу из данных, перебирая каждый из элементов и обращаясь к стейту ответов, по ключю актуально перебираемого элемента,а точнее его АЙДИ и сравниваем если он равен актуально перебираемому элементы ОТВЕТА ( это проще нарисовать)



это все,
Перед тем как что-то возвращать мы проверим что в слайде вообще есть какие-то данные.После этого уже можно делать возврат 
Формируем непосредственно сам JSX
теперь у нас 4 главных блока в слайдере это
1.Счетчик со стрелками
тут каждая стрелка вызывает соотвествующее действие ну и + отоброжаем актуальный слайд

2.обвертка в которой содержатся сами Слайды
Враппер берет ширину 100% от родителя и скрывает все, что выходит за пределы
Внутри блок иннер наоборот растягивается в завивисмоти от того, сколько там слайдов.+ динамически перемещается в зависимости от того, какой слайдер сейчас активен
Далее уже идет непосредтсвенно сам тест.
Перебираем данные через мап, каждому слайду задаем соотвествующий кусочек ширины от  блок иннер.Достаем сам вопрос, посчле чего обращаемся к вариантам ответа и также перебираем их.
Передавая в один лейбл сразу несколько вопросов.
Вопрос можно будет отметить черех инпут тайп Радио
Важные моменты это чтобы ты обьединил их по имени в одну группу + указал его личное значение + позволил отмечать только один ответ.
Также реагировал бы при изменении выбранного ответа
Далее лейбл, указываешь вместо конкретной буквы просто ее номер + индекс ( чтобы буквы менялись динамически)
+ вставляешь сам вариант ответа
3.Индикаторы которые позволяют бегать по слайдам
4.кнопка для завершения теста
